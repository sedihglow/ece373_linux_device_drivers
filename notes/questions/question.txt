1) Do we have to worry about memory fragmentation like on embedded devices with
   little memory?


2) How does the memory gets freed in a way to help with memory fragmentation?


3) What are the error handling functionalities of the kernel? Functions, header,
   does errno.h functions work like in user space?


4) adding packages with no package manager on embedded linux?

5) Should i be using functions like pr_info(), what other print functions are
   there? How to find documentation or a list of functions.

6) Mentioned kernel man pages, where/how to install.

7) What is out of tree vs in tree modules. How to write something that does not
   "taint" the kernel?
